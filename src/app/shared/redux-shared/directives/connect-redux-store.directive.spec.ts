import { ConnectReduxStoreDirective } from './connect-redux-store.directive';

describe('ConnectReduxStoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectReduxStoreDirective();
    expect(directive).toBeTruthy();
  });
});
